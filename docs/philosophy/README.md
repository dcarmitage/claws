# Design Philosophy

The original design philosophy behind claws. These documents inspired the current implementation.

For the working system, see:
- `claws agent onboard` — structured agent training
- `claws curriculum show default` — the default training curriculum
- `claws agent info <name>` — agent identity and evaluation history
