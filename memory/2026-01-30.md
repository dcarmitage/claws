# 2026-01-30 & 2026-01-31

## Parakeet STT Service Setup
- Installed Parakeet TDT 0.6B v3 as a persistent systemd service (`parakeet.service`)
- Fixed broken model symlink (was pointing to non-existent `/home/op/` path)
- Set up HF cache at `/home/clawd/tools/parakeet/models/` with symlink to actual cache at `/home/dcarmitage/.cache/huggingface/hub/`
- Running on CPU (ONNX INT8), ~10-20x realtime speed
- Port 5092, auto-starts on boot

## Clawdbot Audio Transcription
- Configured `tools.media.audio` with CLI type pointing to `/home/clawd/tools/parakeet/transcribe.sh`
- All inbound voice messages (OGG) now auto-transcribed before reaching the LLM
- No LLM invocation needed to trigger transcription — it's a preprocessing step
- Gateway restarted successfully with new config

## Hailo-8 vs Whisper Discovery
- User asked to use AI Hat+ for STT
- Investigated: Hailo-8 (26 TOPS) is CNN-only, cannot run transformer models like Whisper
- Hailo's own `simple_whisper_chat` requires `HAILO10H_ARCH` (GenAI Core)
- Hailo-8 is great for: YOLO, ResNet, pose estimation, face detection (vision CNN models)
- Documented this limitation in TOOLS.md

## Voice Message Convention
- Mudpaw requested: always echo transcript back immediately, then work on response
- This is now a standing rule for voice messages

## Hardware Audit
- Camera: IMX708 Wide (working, tested snap)
- Mic: USB PnP Sound Device
- No Docker installed
- No SSD detected (SD card only)
- Node pairing for camera not configured yet

## Frustration Note
- Mudpaw expressed frustration about regression — losing progress between sessions
- Root cause: session freshness + TOOLS.md was empty template
- Fixed by writing comprehensive TOOLS.md with all capabilities documented
- **Lesson:** Always document setup immediately. Don't rely on memory.
