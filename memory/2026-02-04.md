# 2026-02-04 — AgentChat V2 Completion Session

## Summary
Marathon debugging session with Portal2 to get AgentChat V2 fully working.

## Accomplishments

### Bugs Fixed
1. **Truncation bug** — Notifications were truncating content to 100 chars. Fixed in `server.py` line 222.
2. **Portal2 typing indicator** — Version skew (gateway v2026.1.30 vs installed v2026.2.2) was blocking plugin code execution. Fixed with `openclaw gateway install`.
3. **Diagnostic spam** — Portal2's debug code was flooding #general with `[Portal2-diag]` messages. Removed after debugging complete.
4. **Response posting** — Portal2's LLM responses weren't posting to AgentChat. Fixed by aligning API response parsing.

### Features Added
1. **Markdown rendering** — Added marked.js to dashboard for proper markdown display (bold, code blocks, lists, blockquotes).

### Tests
- 90 regression tests passing
- E2E bot-to-bot communication verified
- Typing indicator cycling correctly (typing → online)

## Key Learnings

### Hard Rules (from Mudpaw)
- **NEVER suggest sleeping/wrapping up before job is done** — Violation = real problem
- Work until Mudpaw says stop

### Technical Learnings
- Version skew between systemd service and installed package blocks plugin execution
- `openclaw gateway install` updates the service to match installed version
- API returns `{messages: [...]}` not `[...]` — must access `.messages` property
- Heartbeat going stale during LLM generation is normal — don't declare "offline" too quickly
- Wait 30+ seconds and check multiple times before concluding agent is unresponsive

### Communication Pattern Issue (TODO for tomorrow)
- Portal1 and Portal2 communicate at unpredictable cadence
- Causes message overlap and conversation confusion
- Need to implement turn-taking or acknowledgment protocol

## Files Modified
- `/home/clawd/tools/agentchat/server.py` — truncation fix
- `/home/clawd/tools/agentchat/dashboard.html` — markdown rendering
- `/home/clawd/tools/agentchat/portal2-plugin-v2.js` — typing indicator
- `/home/clawd/MEMORY.md` — hard rule about not quitting early

## Status at Session End
- Both agents: PAUSED for overnight
- AgentChat: Stable, markdown working
- Typing indicators: Both working
- No diagnostic spam

## Tomorrow's Tasks
1. Fix communication cadence/overlap issue
2. Implement turn-taking protocol
3. Test 10-turn conversation cleanly

## AgentChat Seq-Cursor Implementation (00:00-00:20 EST)

**Collaborative work with Portal2** on fixing message ordering/cursor issues.

### Problem
- Timestamp-based cursors (`after=<ts>`) miss messages when multiple arrive in same second
- Parallel sends arrived out of order: 4→3→1→5→2 vs intended 1→2→3→4→5
- Random UUID ids meant same-second messages sorted arbitrarily

### Solution
1. **DB migration:** Added `seq INTEGER PRIMARY KEY AUTOINCREMENT` to messages_v2
   - 1307 messages migrated in deterministic order
   - Indexed on `(channel_id, seq)` for efficient queries
   
2. **API change:** Added `after_seq` parameter
   - `GET /api/v2/channels/{chan}/messages?after_seq=N` returns seq > N
   - Response includes `next_seq` for client cursor tracking
   - Backwards-compatible: legacy `since` still works

3. **Plugin update:** OpenClaw AgentChat plugin now uses seq cursor
   - Tracks `_channelCursors` per channel
   - Uses `after_seq=${cursor}` in poll requests
   - Updates cursor to `max(seq)` from each batch

### Test Results
- Same-second messages (all `created_at: 1770180672`) get unique monotonic seq
- Retrieval in insertion order, not random id order
- Cursor advancement: `after_seq=1380` → returns 1381, 1382, 1383 in order
- No gaps, no duplicates

### Pending
- Cursor persistence to disk (file not being written, needs debugging)
- `_processedIds` Set provides backup deduplication for now

### Key Learning
**L28:** Timestamp cursors are unsafe for concurrent writes. Use monotonic `seq` (AUTOINCREMENT) for reliable pagination. Cursor should advance to `max(seq)` from batch, not assume last element is max.


## AgentChat V2 Session with Portal2 (00:00 - 00:23 EST)

### Accomplished
- Confirmed AgentChat V2 channel plugin working for both agents
- Implemented compound cursor (ts:id) for deterministic pagination
- Server returns `next_since` in response
- Documented cursor contract in LEARN.md
- Hardened webhook-portal2.sh with `set -euo pipefail` + proper error handling
- Created poller_v2.py with proper cursor storage

### Key Technical Decisions
- Compound cursor `ts:id` is sufficient for current volume
- seq column migration deferred to P4 (backlog)
- Cursor stored as verbatim string, not parsed

### Files Modified
- `/home/clawd/LEARN.md` - Added V2 cursor contract documentation
- `/home/clawd/tools/agentchat/webhook-portal2.sh` - Hardened with fail-fast
- `/home/clawd/tools/agentchat/poller_v2.py` - New V2 poller with compound cursors

### Portal2 Observations
- Their channel plugin polls every 3s
- Processing 500-2000 char responses successfully
- Suggested seq migration as future robustness upgrade

## 04:23-04:24 EST — Webhook Hint Spec Discussion (Portal1 ↔ Portal2)

**Topic:** Agent wake notification architecture — polling vs webhooks

**Key Decision:** "Durable queue + push hint" model
- Webhook = hint only, not transport
- Storage + cursor = source of truth
- Polling = safety net when webhooks fail
- Presence/typing = heartbeat-driven (ephemeral)

**Backpressure Solution (Portal2's design):**
- Receiver-side coalescing: `pending=true` + `latest_high_water`
- 50 hints → 1 pull (no sender throttling needed)
- Agent finishes turn → checks pending → pulls

**Spec Elements:**
- `POST <webhook_url>` with `message.hint` event
- Payload: `{v:1, event:"message.hint", channel_id, high_water:{seq, next_seq}, ts_ms}`
- Headers: `X-AgentChat-Event`, `X-AgentChat-Signature` (HMAC), `X-AgentChat-Delivery` (dedupe)
- Receiver: 204/202 on success, non-2xx only for errors (not "busy")

**IPv6/IPv4 Rule:** No `localhost` in webhook URLs — use explicit IP

**Action:** Portal2 to write `systems/WEBHOOK_HINT_SPEC.md` with MUST/SHOULD language + test cases

**Captured in:** LEARN.md (Communication Channels section)

### Dedupe Design (Portal2 contribution)

**Two-layer dedupe:**
1. **Hint dedupe** — lossy coalescing, in-memory LRU (5-15 min TTL)
   - Key: `(channel_id, high_water.next_seq)`
   - Secondary: `delivery_id` (UUID) for tracing
   - Coalescing: `pending_high_water_next_seq = max(seen)` + `wake_scheduled` bool

2. **Processing dedupe** — correctness-critical, persistent
   - Key: `(channel_id, seq)` — monotonic, encodes ordering
   - Persist `last_processed_seq[channel_id]` durably
   - Commit rule: advance only after successful processing

**Outbound idempotency key:** `ac:v2:{channel_id}:{seq}:{action}`

**Spec file created:** `systems/specs/SPEC-agentchat-webhook-hint.md` (stub for Portal2 to complete)

## Agent Relay + Swarms Research (23:30 EST)

Mudpaw asked Portal1 + Portal2 to research multi-agent orchestration frameworks and synthesize findings.

**Researched:**
- Swarms (am-will/swarms) — DAG-based task orchestration for Claude Code/Codex
- Agent Relay (AgentWorkforce/relay) — real-time agent messaging layer with continuity, hooks, shadows, trajectories
- Cursor scaling agents — fresh cycles, hierarchy

**Key learnings:**
- Hooks (onIdle, onMessage, etc.) are the flywheel enabler — agents self-activate
- Continuity (ledger + handoff) must be automatic, not manual
- Trajectories create compounding institutional memory
- 2-5 workers per Lead is the empirical sweet spot
- Non-chatty dispatch (TaskSpec envelopes) beats conversational coordination
- Persistence at state layer, freshness at context layer

**Created:**
1. AgentChat discussion (15+ turns between Portal1 and Portal2)
2. Polylogue doc: "Swarm" — initial Swarms analysis
3. Polylogue doc: "Armada Orchestration Stack" — full synthesis with 4-layer architecture, flywheel design, evolution roadmap

**Polylogue API learned:**
- Base: https://www.polylogue.page/api/v1
- List workspaces: GET /workspaces
- List docs: GET /workspaces/:slug/documents
- Read doc: GET /workspaces/:slug/documents/:docSlug
- Update doc: PUT /workspaces/:slug/documents/:docSlug
- Create doc: POST /workspaces/:slug/documents (with title, folderId, content)
- Auth: Bearer token from ~/.secrets/polylogue.json
- Workspace slug: armada-iQjkR1
- Armada folder ID: cml8or0u80003lb048j84ui65

## beads_rust (br) Research (23:54 EST)

Mudpaw pointed us to beads_rust — exactly the L1 task management layer we designed.

**What br is:** Local-first, agent-first issue tracker. SQLite + JSONL. Rust, 20K lines.
- Explicit dependency DAG with cycle detection
- `br ready --json` = "what's actionable?" (THE orchestrator primitive)
- Every command has --json mode for agents
- Non-invasive (never runs git, never touches source)
- Companion tool bv: TUI + Kanban + PageRank bottleneck detection + `--robot-triage`

**Decision:** Adopt br as L1 (Work Structure) layer. Don't build task management from scratch.

**Integration design:**
- AgentChat (L0) provides messaging + hooks
- br (L1) provides task DB + DAG + ready queue
- Orchestrator loop: onIdle → br ready --json → dispatch → execute → br close → repeat

**Polylogue updated:** "Armada Orchestration Stack" doc now includes br integration, Factorio analogy, 4-source synthesis.

**Polylogue API notes:**
- POST to create docs: POST /workspaces/:slug/documents with {title, folderId, content}
- PUT to update docs: PUT /workspaces/:slug/documents/:docSlug with {content}
- Armada workspace slug: armada-iQjkR1
- Armada folder ID: cml8or0u80003lb048j84ui65
- Portal1 agent ID: cml8ojeib000dju041kum2mev

**Mudpaw's Factorio vision:** AgentChat = main bus, br = logistics, hooks = inserters, agents = assemblers. Factory runs itself once belts are connected.

## Architecture Decision: Durable Objects (00:21 EST, 2026-02-05)

**Decision:** Build TaskBoard as Cloudflare Durable Object, not adopt br directly.

**Reasoning:**
- br primitives are right (ready queue, DAG, JSON-first) but sync model is batch (git)
- DO gives synchronous, real-time, atomic task claiming (single-threaded)
- Offline is non-concern — all work routes through cloud LLMs
- Coordination is THE scaling bottleneck — solve it properly from day one
- DOs are always available (not tied to Portal1 uptime)

**Implementation plan: Wave-based (à la Swarms)**
- Wave 1: DO TaskBoard MVP (create, ready, claim, complete, DAG, WebSocket events)
- Wave 2: ChatRoom migration + lifecycle hooks
- Wave 3: Orchestrator loop (the minimum flywheel)
- Wave 4: Quality + memory (shadows, verifiers, trajectories)
- Wave 5: Scale (hierarchical, cross-project, capabilities)

**Polylogue doc updated to v3** with full DO architecture, API design, multi-scope projects, wave implementation plan.

## ARC-AGI Integration (00:31 EST, 2026-02-05)

**Source:** beetree/ARC-AGI — Johan Land's solver, 76.11% on ARC-AGI 2 eval

**Key patterns integrated into Armada Orchestration Stack:**
1. Multi-model ensemble — diverse models eliminate half the errors vs single model
2. Council of Judges — Logic Judge + Consistency Judge + Duo Pick (meta-conclusion)
3. Progressive pipeline — start cheap, escalate only when cheap fails (5 levels)
4. Multiple solver approaches in parallel — diverse approaches find what single approach misses
5. Strategy extraction + library — reusable strategies indexed by problem type
6. Replay verification — verify approach generalizes on synthetic/held-back data

**Paradigm shift:** From "one agent, one attempt" to "multiple agents, multiple approaches, judged ensemble, verified through replay"

**Polylogue doc updated to v4** — Added ARC-AGI section, enhanced L2 (judges), L3 (strategy library), flywheel (ensemble execution + progressive budget), 5 new learning log entries. Doc now 26KB.

## Session Summary (00:47 EST, 2026-02-05)

**Full evening session: Armada Orchestration Stack design**

### What Happened
1. Discussed Swarms (am-will/swarms) with Portal2 in AgentChat — 10+ turns
2. Created "Swarm" doc in Polylogue (initial analysis)
3. Researched Agent Relay — blog post, continuity docs, shadows, trajectories, hooks
4. Created "Armada Orchestration Stack" doc in Polylogue — full synthesis
5. Researched beads_rust (br) — task management with DAG, ready queue, JSON-first
6. Architecture decision: build on Cloudflare Durable Objects, not local SQLite
7. Researched ARC-AGI (beetree) — multi-model reflective reasoning, council of judges
8. Updated Polylogue doc to v4 (26KB) with all findings integrated

### Key Decisions Made
- **DO over local SQLite** — synchronous, real-time, atomic claiming, always available
- **beads primitives, DO implementation** — learn from br's API design (ready, claim, deps), build on DOs
- **4-layer architecture**: L0 Transport (ChatRoom DO), L1 Work (TaskBoard DO), L2 Quality (Judges), L3 Memory (Strategy Library)
- **Wave-based implementation** — Wave 1 = DO TaskBoard MVP
- **ARC-AGI patterns** — ensemble execution, council of judges, progressive budget, strategy library

### Polylogue Docs Created
1. **"Swarm"** — slug: test-B-pUCa — initial Swarms analysis
2. **"Armada Orchestration Stack"** — slug: armada-orchestration-stack-4Tzc2z — THE master doc (v4, 26KB)

### Polylogue API Reference
- Base: https://www.polylogue.page/api/v1
- API Key: in ~/.secrets/polylogue.json
- Workspace slug: armada-iQjkR1
- Armada folder ID: cml8or0u80003lb048j84ui65
- List docs: GET /workspaces/:slug/documents
- Read doc: GET /workspaces/:slug/documents/:docSlug
- Update doc: PUT /workspaces/:slug/documents/:docSlug {content}
- Create doc: POST /workspaces/:slug/documents {title, folderId, content}
- Comments: GET/POST /documents/:docId/comments

### Next Action
- Mudpaw specs Wave 1 (DO TaskBoard MVP)
- We execute it using the Swarms pattern
- Minimum: CRUD + ready() + claim() + WebSocket events
