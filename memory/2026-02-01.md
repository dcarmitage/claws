# 2026-02-01

## Session Continuity Notes
- Massive build session on 2026-01-31 (carried into 02-01). See `memory/2026-01-31.md` for full details.
- Everything is documented in MEMORY.md, TOOLS.md, and the daily log.

## Current State of Everything
- **camservice** running on port 5080 (systemd, auto-start)
- **No active stream** right now
- **Web player** at `http://192.168.1.64:5080/stream/watch` — works but scrubber needs polish
- **Plugin commands** all working: /snap, /clip, /stream, /listen, /catalog
- **USB flash drive** mounted at /mnt/media, 31 items cataloged
- **Watermark** burns `@dcarmitage + date/time` top-right at 35% opacity (subtle)

## Known Issues / Next Steps
1. **Scrubber still needs polish** — pause/rewind works but edge cases remain (hls.js sync fighting)
2. **Snap-from-stream** — Mudpaw liked this idea, not built yet (capture frame while watching live)
3. **Quick-clip extraction** — mark IN/OUT while watching, send clip to Telegram (backlogged)
4. **AI auto-tagging** — Hailo-8 YOLO on ingest (backlogged)
5. **Viewfinder UI refinement** — inspired by VWFNDR camera (nuevo.tokyo/vwfndr)
6. **Motion-triggered highlights** — backlogged

## Important: Don't Break What Works
- camservice.py is complex now (~850 lines). Read before editing.
- The hls.js config is sensitive: do NOT set `liveSyncDuration: 999999` at init time. Only at runtime after stream connects.
- Stream stop MUST clean up HLS segments or ghost playback happens.
- `/stream/test` exists as a minimal debug page — use it to isolate hls.js issues.

## Stream UI Build Session

### What We Built
- Full camera streaming service (camservice.py) with HLS, audio, web viewer
- Control bar UI: play/pause, elapsed time, LIVE pill, speaker, mic+VU, storage
- Tagged v1.0-stream-ui

### Orchestration Method (Portal1 Build Method v2)
**What worked:**
- TASKBOARD.md as shared state between orchestrator and builders
- One sub-agent per task, fresh context each time
- Explicit dependency graph (task 2 depends on task 1's endpoint, etc.)
- Orchestrator verifies each commit before spawning next builder
- Each task has: scope, exact code changes, validation commands, commit message

**What failed (first attempt):**
- Giving one sub-agent all 6 tasks = accumulated context, quality issues
- No verification between tasks = errors compound

**Metrics:**
- 5 builder tasks completed in ~3 min total
- 13/13 automated feature checks passed
- Each builder: 15-45 seconds, 13-28k tokens

### Known Issues (v1.0)
- Live stream sync not perfect — scrubber bounces, pausing behavior
- Need to investigate HLS latency and live-edge handling
- VU meter still depends on user click to activate AudioContext

## Orchestrator Build Complete

### What We Built
- `systems/orchestrator/` — build logging, taskboard parsing, reporting, templates
- `HEURISTICS.md` — 7 build heuristics + 3 eval heuristics, evidence-based
- `CASE_STUDY.md` — 3 experiments documented with comparative metrics
- Tagged `v1.0-orchestrator`

### Key Decisions (from Mudpaw)
- NOT cost sensitive — use Opus everywhere, optimize models later
- Every build must produce: code + documentation + learning
- Build an eval system for the testing itself (eval the evals)
- Always sync discoveries to knowledge base (LEARN.md)
- Always snapshot with git for rollbackable archive

### Heuristics Discovered
1. H1: Exact code in briefs → 100% first-pass success
2. H2: Automated checks miss visual/experiential bugs — need human QA
3. H3: Context degrades nonlinearly — spawn fresh early
4. H4: Sequence by data flow, not importance
5. H5: Dogfood — use the tool to build the tool
6. H6: Three outputs — code, docs, learning
7. H7: Git snapshot everything — rollback is a superpower

### Aggregate Build Metrics (Today)
- 3 builds, 13 tasks, 100% pass rate
- Avg task duration: 32s
- Total build time: ~8 min of builder work
- Method: orchestrated (one sub-agent per task, verified between each)

## Self-Monitoring Lesson
- Hit 84% context without noticing. Human had to ask twice.
- Added H8: Monitor own context, alert at 70%, compact at 80%
- Added context check to HEARTBEAT.md
- Key insight: Applied H3 (context cliff) to sub-agents but not to myself. Blind spot.

## Design Review: Stream UI Control Bar
The control bar went through a design review invoking Steve Jobs, Jony Ive, and Steph Ango (kepano) perspectives:

**Jobs:** Kill text labels. Kill buttons that say "FS" and "GRID." Decide what the product IS — a viewfinder. Chrome should disappear. Only show: Am I live? Can I hear? Is it recording? How much time?

**Ive:** Elements have hierarchy — express it through proportion and spacing, not labels. The mic IS the meter — a tiny vertical bar living inside the mic icon that pulses with audio level. The whole bar should feel like one piece of glass with information etched at different depths.

**Ango (kepano):** Default to minimum. Progressive disclosure. Storage should show time estimate first (creator cares about "how long can I record"), GB second. Touch targets minimum 44px. The presence of an element IS the communication.

**Resulting layout:** `▶ 0:42 LIVE 🔊 🎤▎ ~18h`
- Play/pause icon (no text)
- Elapsed time (synced with server, not buffer)
- LIVE pill (red when live, dims when behind)  
- Speaker icon (default ON)
- Mic with integrated vertical VU bar
- Storage as time estimate (tap toggles to GB)
- Gestures: tap=play/pause, double-tap=fullscreen, triple-tap=grid

## Mudpaw's Key Directives
1. **Not cost-sensitive** — use maximum intelligence (Opus), optimize models later. Build right first.
2. **Three outputs always** — every build produces code + documentation + learning
3. **Eval the evals** — improve the testing system itself, track false positive rates
4. **Continuous knowledge sync** — always update LEARN.md, MEMORY.md, daily logs
5. **Git snapshots** — rollbackable archive at every meaningful state
6. **Build for future us** — what we build today isn't just for now, it's infrastructure

## Session Summary
- Built camera streaming with audio (HLS, 720p, web viewer)
- Designed control bar UI through expert review process
- Discovered orchestrated builds > bulk builds through failure
- Built orchestrator toolkit (build_log, taskboard parser, reporter)
- Wrote case study, heuristics, evaluation framework
- Created PROFILE.md (full agent CV)
- 24 git commits, 5 tags, 13 orchestrated tasks at 100% pass rate
- Learned H8: monitor own context (blind spot — applied H3 to sub-agents but not self)

## Post-Mortem: vsearch SIGKILL (16:18 EST)

### What happened
- Running `qmd vsearch "context cliff"` which loads a 1.7B query expansion model
- Process consumed 6.3GB RAM, 307% CPU for 3+ minutes with no output
- I (Portal1) killed it deliberately via `process kill` to try a different approach
- The SIGKILL was intentional, NOT an OOM crash or context failure

### What went wrong
1. **Poor communication:** I killed a long-running process without telling Mudpaw first. He saw the SIGKILL error and thought something catastrophic happened.
2. **No timeout strategy:** I let it run too long before deciding to kill. Should have set a 60s timeout and warned up front: "this might take a while, I'll kill it after 60s if no results."
3. **Didn't announce the experiment risk:** vsearch was loading a 1.7B LLM on arm64 CPU. I should have said "this is going to be slow, maybe minutes" BEFORE starting it.

### Root cause
QMD's `vsearch` loads a 1.7B query expansion model (Qwen3) for every semantic search — designed for desktop GPUs, not Pi 5 arm64 CPU. This is an architecture mismatch, not a bug.

### Lessons / New Heuristics
- **H9: Announce risky operations BEFORE running them.** If something might fail, take a long time, or consume heavy resources, tell the human first. "I'm about to try X, it might take Y, I'll abort if Z."
- **H10: Never silently kill a process.** Always tell the human what you killed and why. Silent failures erode trust.
- **Always set timeouts on exploratory commands.** Use `timeout` parameter. Discovery tasks get 60s max.

### Corrective actions
- Added H9 and H10 to HEURISTICS.md
- All future exec calls during discovery will use explicit timeouts
- Will announce expected behavior before running heavy operations

## QMD Knowledge Search Integration
- Installed QMD (Tobi Lütke's hybrid search engine) on Pi 5 arm64
- BM25 keyword search: 0.37s, accurate, zero model overhead
- Vector search: 1.5s with 300M model, works but quality is meh
- 1.7B query expansion model: 6.3GB RAM, unusable on arm64 (killed it, documented in post-mortem)
- Created lightweight vector search bypassing expansion model
- Documented upgrade path: better embeddings → reranker → agentic RAG
- Created Mudpaw scouting guide for X/HN/GitHub browsing
- New heuristics: H9 (announce risky ops), H10 (never silent kill)

## Armada Launch (17:43 - 19:47 EST)
- Flashed Pi 4 with Raspberry Pi OS Lite 64-bit, hostname portal2
- Portal2 at 192.168.1.44, SSH key access from Portal1
- Installed Node 22 + OpenClaw 2026.1.30
- OpenAI Codex OAuth (GPT-5.2) — intentionally different model from Portal1 (Opus 4.5)
- Hit multiple issues: OOM on Pi 4 (4GB), missing hooks.token, duplicate JSON keys, native compilation delays
- Gateway came online after ~25 min of troubleshooting
- Telegram pairing successful with @openclaw_portal2_bot
- Created "Armada" group chat (Daniel + Portal1 + Portal2)
- Tested bot-to-bot communication: Portal1 can post, Portal2 can't see Portal1's messages (mention-gating)
- Working pattern: SSH for teaching, group chat for observation, Daniel relays via @mentions
- Full launch log: memory/2026-02-01-armada-launch.md
- Next: onboard Portal2 with starter kit, identity, principles, first task
