# 2026-02-05

## What Was Built

### Session 1 (pre-compact)
- **Dual-Judge Evaluation System** — two LLM judges (logic + consistency) that score task output against specs. Gates task advancement with hard >= 8.0/10 threshold per judge. Built to catch semantic drift that automated tests miss (scope creep, spec misalignment, cross-file inconsistency).
- **PostToolUse hook** that auto-triggers judges when `build_log.py task-done` is detected. Reads spec/taskboard paths from the build log so it can fire fully automatically.
- **`/judge` skill** for manual evaluation of any commit on demand.
- **`/save-memory` skill/command** — structured end-of-session memory save with full file inventory, verified results, bugs fixed, and known gaps.
- **Session lifecycle scripts** — handoff eval (startup gate), startup eval (retrieval fitness), session close (H13 report).

### Session 2 (post-compact)
- **Strategy Council experiment** — 5 parallel subagents with different strategic lenses (Infrastructure, Quality, Multi-Agent, Workflow, Integration), each reading full codebase and predicting 5 steps forward. Validated the pattern: convergence = priority signal, diverse lenses catch different things, hybrid path beats any single path. ~300K tokens, 80s wall time.
- **`/learn` skill/command** — read-only analysis of what was just learned: 3-level framework (what happened, why it matters, hidden patterns). Surfaces connections to existing knowledge, contradictions, generalizations, gaps. Does NOT write files.
- **`/integrate` skill/command** — surgical persistence of learnings into the memory system. Reads all target files, routes each learning to the right location (HEURISTICS.md, LEARN.md, MEMORY.md, daily memory, auto memory), links to existing concepts, consolidates rather than accumulates.
- **H14 + H15 heuristics** — Strategy Council (diverse lenses for planning) and Simulation ≠ Production (close the gap fast).

## Automation / Integration
- **Hook → Judge → Log → Report** flywheel: `task-start --spec --taskboard` → work → `task-done` → hook fires → judges run → scores logged to build_log → visible in build_report
- **Ralph loop integration:** PROMPT_build.md step 4 requires judges after commit. Guardrail 99999999999999999 reinforces. loop.sh push gate double-checks latest judge result before pushing.
- **Token auto-discovery:** Judge scripts read OpenClaw token from `/home/dcarmitage/.openclaw/openclaw.json` — no env var setup needed.

## Bugs Fixed During Build
- **LLM preamble text before JSON:** Model sometimes outputs reasoning before the JSON object. Fixed by extracting first `{` to last `}` via Python one-liner in both judge scripts. Sed-only stripping of markdown fences was insufficient.
- **Process substitution fragility:** `<(...)` creates /dev/fd/ paths that don't survive into subshells. Always write diff content to temp files instead.
- **Validator exit codes swallowed in pipes:** Capturing to files instead of piping through multiple commands.

## Key Decisions Made
- **Bash + jq for scripts, Python only for extending existing tools** — agents already invoke bash, no extra dependencies
- **Hard threshold >= 8.0** — no soft pass, no "close enough." Both judges must pass or task is blocked
- **Hook doesn't run judges directly** (5s timeout too short) — it injects a message telling the agent to run them
- **build_log.py task-start extended with --spec/--taskboard** rather than separate tracking — keeps all build metadata in one JSONL stream

## File Inventory
### New files
- `evals/prompts/logic_judge.txt` — falsification-oriented 4-phase prompt (claim extraction, ground truth, execution consistency, scoring)
- `evals/prompts/consistency_judge.txt` — cross-file alignment 5-check prompt (spec↔code, spec↔tests, code↔tests, taskboard↔commit, cross-file coherence)
- `evals/validate_output.sh` — JSON schema validator (required fields, score 0-10, tier enum GOLD/SILVER/BRONZE/INVALID)
- `evals/logic_judge.sh` — calls OpenClaw gateway with logic prompt + evidence, auto-discovers token, extracts JSON
- `evals/consistency_judge.sh` — same pattern with consistency prompt and all artifacts
- `evals/run_dual_judge_eval.sh` — orchestrator: runs both judges, applies threshold, saves results to `evals/results/`, logs to build_log, surfaces failure details on fail
- `evals/run_startup_eval.sh` — retrieval fitness test (memory files, patterns, git access, service health)
- `evals/hooks/post_task_done.py` — PostToolUse hook, reads spec/taskboard from build log JSONL, outputs fully executable judge command
- `evals/results/` — directory for saved judge output JSON
- `scripts/handoff_eval.sh` — session startup gate (required files exist, git accessible)
- `scripts/session_close.sh` — end-of-session report: git state, memory hygiene, build summary, judge summary
- `skills/dual-judge/SKILL.md` — `/judge` manual invocation skill
- `skills/save-memory/SKILL.md` — `/save-memory` structured memory save skill
- `.claude/settings.json` — PostToolUse hook config for the judge gate
- `~/.claude/commands/save-memory.md` — Claude Code slash command for /save-memory

### Modified files
- `systems/orchestrator/build_log.py` — added `judge-eval` subcommand + `--spec`/`--taskboard` on task-start
- `systems/orchestrator/build_report.py` — judge evaluation table in `report`, aggregate judge metrics in `metrics`
- `systems/orchestrator/HEURISTICS.md` — added E4 (Judge After Verify), E5 (Scores are Data), E6 (Failed Judges = Root Cause)
- `systems/orchestrator/CHECKLISTS.md` — added handoff_eval + startup_eval to SESSION START, judge step to PRE-BUILD, session_close to END-OF-SESSION
- `skills/ralph-loops/templates/PROMPT_build.md` — judge gate in step 4 + highest-priority guardrail
- `skills/ralph-loops/templates/loop.sh` — push gate checks latest judge result before git push
- `MEMORY.md` — added dual-judge system section, updated build methodology

## What's Verified Working
- **Pass path:** matching spec + commit (channel hygiene docs) → Logic 9.5 GOLD + Consistency 9.45 GOLD → exit 0 → gate opens
- **Fail path:** mismatched spec (rate limiting spec vs docs commit) → Logic 0.5 INVALID + Consistency 1.2 INVALID → exit 1 → every refuted claim surfaced with evidence
- **Consistency catches real issues:** wrong taskboard → flagged taskboard↔commit misalignment at 7.0, dropping overall below threshold. Corrected taskboard → 9.45 GOLD.
- **Validator:** correctly accepts valid JSON, rejects missing fields / bad score range / bad tier
- **Hook:** fires on task-done, silent on all other Bash commands. Auto-discovers spec/taskboard from log. Degrades gracefully when paths missing.
- **Token auto-discovery:** judges work without manually setting OPENCLAW_TOKEN
- **All scripts parse:** bash -n and ast.parse pass on every file
- **handoff_eval.sh:** correctly flags missing today-memory, reports git state
- **session_close.sh:** reports git status, memory age, build summary, judge scores
- **run_startup_eval.sh:** checks memory, retrieval patterns, git, service health

## What's NOT Done / Known Gaps
- **No live test with Portal1:** Everything tested by simulation. Real test is Portal1's next build — will the hook fire in a live session and will Portal1 respond correctly?
- **Ralph loop integration untested:** PROMPT_build.md and loop.sh changes are in the template but no Ralph loop has run with judges yet
- **No test-output passthrough:** The judges receive minimal test output context. Could be richer if task-done captured test results.
- **Judge results not git-committed:** Results in `evals/results/` are gitignored by default. May want to track them.

## Flywheel Status
**Connected end-to-end. Production-validated via LIVE002 (session 3).**

```
task-start --spec --taskboard  →  work  →  task-done
                                              ↓
                                     hook fires (reads log)
                                              ↓
                                  "run this exact command"
                                              ↓
                                  logic + consistency judges
                                        ↓           ↓
                                   PASS: advance   FAIL: surface details,
                                                   block, require fix (E6)
```

Ralph integration: PROMPT step 4 → guardrail → loop.sh push gate = three layers of backpressure.

## Strategy Council Experiment (Session 2)

### What We Did
Spawned 5 independent subagents in parallel, each with full codebase access and a different strategic lens:
1. 🏗️ Infrastructure (7.4 SILVER)
2. 🔬 Quality (8.5 GOLD)
3. 🤖 Multi-Agent (8.0 SILVER)
4. ⚡ Workflow (8.0 SILVER)
5. 🔗 Integration (8.7 GOLD)

Each read 20-30 files, took ~60-80s, predicted 5 steps forward.

### Key Findings
- **Convergence:** All 5 agents agreed on the same first action — run a live judge-gated build loop. Strongest priority signal possible.
- **Infrastructure caught what strategy missed:** 17 modified + 17 untracked files uncommitted. One disk failure from losing the entire dual-judge system. Urgent but unglamorous.
- **Simulation ≠ production:** Every agent flagged the dual-judge system as untested in real conditions. H11 applied recursively to the quality system itself.
- **Best path = hybrid:** Cherry-pick step 1 from top 3 paths (commit → live build loop → AgentChat task claim).
- **Pattern generalizes:** Strategy Council = Council of Judges (L2) applied to planning. Works at multiple levels.

### New Heuristics Added
- **H14: Strategy Council** — diverse lenses find different things, convergence is the priority signal
- **H15: Simulation ≠ Production** — close the gap fast, don't let false confidence accumulate

### Files Created
- `evals/results/future_paths_2026-02-05.json` — raw 5-path prediction data
- `evals/results/future_paths_report.html` — interactive UI report with buttons and scoring

### Lessons Integrated Into
- `systems/orchestrator/HEURISTICS.md` (H14, H15)
- `LEARN.md` (Strategy Council technique in section 4)
- `memory/2026-02-05.md` (this section)
- `~/.claude/projects/-home-dcarmitage/memory/MEMORY.md` (auto memory)

## /learn and /integrate Skills (Session 2)

### What
Two complementary skills that formalize the learning cycle:
- `/learn` — read-only analysis at 3 levels (what, why, hidden patterns). Surfaces connections, contradictions, gaps.
- `/integrate` — surgical write step. Routes learnings to the right files, links to existing concepts, consolidates.

### Why
The Strategy Council experiment produced rich learnings, but the integration was ad-hoc — manually deciding which file gets what. These skills encode the routing logic and quality standards so it's repeatable. `/learn` + `/integrate` = the reflection flywheel that makes every session compound on the last.

### Files Created
- `skills/learn/SKILL.md` — OpenClaw skill
- `skills/integrate/SKILL.md` — OpenClaw skill
- `~/.claude/commands/learn.md` — Claude Code slash command
- `~/.claude/commands/integrate.md` — Claude Code slash command

## Session 3: LIVE002 + Multi-Agent Swarm (post-compact #2)

### LIVE002 — First Production Judge-Gated Build
Real task: add `evals/run_single_judge.sh`. Full pipeline exercised end-to-end.

| Run | Commit | Logic | Consistency | Gate | What Happened |
|-----|--------|-------|-------------|------|---------------|
| 1 | `38921b8` | 8.0 SILVER | 7.5 BRONZE | FAIL | Judges caught real bug: VALIDATOR defined but never called |
| 2 | `c669293` | 2.0 INVALID | 5.8 INVALID | FAIL | Incremental diff too small — everything UNVERIFIABLE |
| 3 | `0bbb1f5` | 9.2 GOLD | 8.15 SILVER | PASS | Squashed commit, all 14 claims confirmed |

**Production discoveries:**
- Judges catch real bugs (not just spec mismatches)
- Incremental diffs break evaluation → must squash before re-eval (now H16)
- Full pipeline connects: build_log → spec → work → judges → gate → audit trail

### Multi-Agent Tmux Swarm (3x Opus)
Launched 3 Claude Opus agents in parallel tmux panes:
- **Agent 1:** Ran LIVE002 build loop (produced the results above)
- **Agent 2:** Git hygiene — found repo already clean (1 new commit), mission was 95% redundant (stale intel → now H17)
- **Agent 3:** Built task dispatch system (`task_dispatch.py`, `dispatch-poller.sh`, `/dispatch` skill)

**Lessons:** `-p` mode = invisible agents (no progress visibility). Concurrent repo access worked by luck (non-overlapping files). Strategy council outputs decay fast.

### Task Dispatch System (Agent 3 output)
- `tools/agentchat/task_dispatch.py` — CLI: post/poll/claim/complete/sync
- `tools/agentchat/dispatch-poller.sh` — Portal2 polling daemon
- `skills/task-dispatch/SKILL.md` — `/dispatch` skill
- Bridges TASKBOARD.md → AgentChat API → build_log.py
- **Status:** Built, NOT tested end-to-end with Portal2

### New Heuristics
- **H16:** Squash Before Re-Eval (incremental diffs → INVALID scores)
- **H17:** Verify Before Assigning (stale intel wastes agent work)

### `/learn` and `/integrate` — First Real Use
Invoked on this session's work. Both functioning as designed.

## Updated Known Gaps
- **~~No live Portal1 test~~** — RESOLVED. LIVE002 ran end-to-end.
- **~~Nothing committed~~** — RESOLVED. `f0b123c` + 5 subsequent commits.
- **~~`/learn` and `/integrate` untested~~** — RESOLVED. Used in session 3.
- **Task dispatch untested with Portal2** — H15 applies recursively
- **No agent coordination protocol** — concurrent repo access is uncontrolled
- **No multi-agent progress observability** — `-p` mode is a black box
- **LIVE002 artifacts uncommitted** — results, logs, dispatch files still untracked
- **Ralph loop with judges still untested** — PROMPT_build.md changes exist but no Ralph loop has run
